# https://www.cloudreach.com/en/resources/blog/containerize-this-how-to-use-php-apache-mysql-within-docker-containers/
#https://stackoverflow.com/questions/37015624/how-to-run-a-cron-job-inside-a-docker-container
version: "3.2"
services:
  php:
    container_name: sla_php
    build: './php/'
    environment:
      - STATUSLIGHT_ENV
    networks:
      - backend
    volumes:
      - ./src/:/var/www/html
  nginx:
    container_name: sla_nginx
    build: './nginx/'
    networks:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./src/:/var/www/html
      - ./letsencrypt/:/etc/letsencrypt/
    depends_on:
      - php
  composer:
    container_name: sla_composer
    image: composer:latest
    command: ["composer", "install", "--ignore-platform-reqs"]
    volumes:
      - ./src/:/app

networks:
  frontend:
  backend:
